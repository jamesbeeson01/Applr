% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geom_slice_caption.R
\name{geom_slice_caption}
\alias{geom_slice_caption}
\title{Describe the lines drawn by geom_slice() in the plot caption}
\usage{
geom_slice_caption(model = TRUE, prepend = "", append = "", format = NULL)
}
\arguments{
\item{model}{If \code{TRUE} (default), the subtitle's first line is the model
equation; \code{FALSE} drops it, leaving only the held-values line.}

\item{prepend, append}{Plain strings pasted before the first line and after
the last line of the default subtitle.}

\item{format}{A function of \verb{(equation, values)} — the ready-made equation
string and the named list of unlabeled held values — returning the whole
subtitle. When given, it fully replaces the default layout (\code{model},
\code{prepend}, and \code{append} are ignored).}
}
\value{
An object that sets the plot caption when added to a ggplot.
}
\description{
\code{geom_slice_caption()} is \code{\link[=geom_slice_subtitle]{geom_slice_subtitle()}} for the plot caption: it
fills the caption (right-aligned in ggplot2's default themes) with the
model equation and the held values the plot does not otherwise show. All
behavior — what is reported, what is skipped because the legend, facets,
or \code{\link[=geom_slice_text]{geom_slice_text()}} already label it, and every parameter — matches
\code{\link[=geom_slice_subtitle]{geom_slice_subtitle()}}; see its documentation for the details.
}
\examples{
library(ggplot2)

# Caption reports the model equation and that hp is held at its mean
model <- lm(mpg ~ disp + hp, data = mtcars)
ggplot(mtcars, aes(disp, mpg)) +
  geom_point() +
  geom_slice(model) +
  geom_slice_caption()

}
\seealso{
\code{\link[=geom_slice_subtitle]{geom_slice_subtitle()}} for the full documentation of what is
reported; \code{\link[=geom_slice]{geom_slice()}} for the layers being described;
\code{\link[=geom_slice_text]{geom_slice_text()}} to label the lines themselves.
}
