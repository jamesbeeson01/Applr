% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/slice_explore.R
\name{slice_explore}
\alias{slice_explore}
\title{Explore a linear model with sliders}
\usage{
slice_explore(
  model,
  sliders = TRUE,
  slider_breaks = NULL,
  slider_steps = NULL,
  ...,
  mapping = NULL,
  data = NULL,
  x_axis = NULL
)
}
\arguments{
\item{model}{A linear model fitted by \code{\link[=lm]{lm()}} \strong{with a \code{data} argument}.}

\item{sliders}{Which predictors get sliders. \code{TRUE} (default) gives one to
every predictor the plot does not otherwise show. Or name them: a bare
name (\code{sliders = cyl}), several (\code{sliders = c(disp, cyl)}), strings
(\code{sliders = "cyl"}), or \code{factor(cyl)} to step a numeric predictor through
its observed values instead of a continuous range.}

\item{slider_breaks}{Exact values for a slider's steps — intentionally
jumpy, where the default grid feels continuous. A named list such as
\code{slider_breaks = list(cyl = c(4, 6, 8))}, or a bare vector when there is
only one slider.}

\item{slider_steps}{How many steps a continuous slider's automatic grid
gets: a single number for all sliders or a named list, e.g.
\code{slider_steps = list(hp = 15)}. The default aims for smooth dragging
while keeping the precomputed curves light.}

\item{...}{Passed on to \code{\link[=geom_slice]{geom_slice()}} — for example
\code{predict_vars = list(hp = 110)} to hold an unslid predictor at a chosen
value, or \code{interval = "confidence"} for a ribbon that moves with the
line.}

\item{mapping}{Extra aesthetics created with \code{\link[ggplot2:aes]{ggplot2::aes()}}, as in
\code{\link[=autoplot.lm]{autoplot.lm()}} — e.g. \code{mapping = aes(color = factor(cyl))} pins \code{cyl}
per color group (and its slider is dropped, since the plot now shows it).
Only the color aesthetic is displayed.}

\item{data}{A data frame to draw the scatter from instead of the data
recovered from the model, as in \code{\link[=autoplot.lm]{autoplot.lm()}}.}

\item{x_axis}{The name of the predictor to place on the x-axis. Defaults to
the model's first numeric predictor.}
}
\value{
A plotly htmlwidget: the scatter, the slice line(s), and one
slider per explored predictor.
}
\description{
\code{slice_explore()} turns a fitted \code{\link[=lm]{lm()}} into an interactive plot: the
model's data as a scatter with a \code{\link[=geom_slice]{geom_slice()}} prediction line through it,
plus one slider for each predictor the plot does not show. Dragging a
slider moves the line to the slice at that value in real time, so you can
\emph{feel} how the hidden predictors bend and shift the fit — where the static
plot holds them at one imputed value, the sliders let you sweep them.
}
\details{
Every curve a slider can show is precomputed by \code{geom_slice()} itself (the
sliders simply choose among slices of the same model), so the lines here
and the lines in \code{\link[=autoplot.lm]{autoplot.lm()}} / \code{\link[=geom_slice]{geom_slice()}} always agree — including
imputation of unslid predictors, back-transformation of a transformed
response, and per-group pinning through \code{mapping = aes(color = ...)}.
Each slider starts at the value \code{geom_slice()} would impute (mean, or most
common level), so the initial view \emph{is} the static autoplot.

Numeric sliders feel continuous by default (a fine grid over the observed
range). For deliberate, chunky steps give \code{slider_breaks}; for steps at a
numeric variable's observed values wrap it in \code{factor()}; factor and
character predictors step through their levels automatically.

The result is a regular plotly htmlwidget: it displays in the RStudio
Viewer, and renders fully interactive (no R session needed) in R Markdown /
Quarto HTML output.
}
\examples{
if (interactive()) {
  model <- lm(mpg ~ disp + hp + wt, data = mtcars)

  # Sliders for hp and wt (disp is on the x-axis)
  slice_explore(model)

  # ... the same, via autoplot
  autoplot(model, sliders = TRUE)

  # Only slide hp; hold wt at a chosen value
  slice_explore(model, sliders = hp, predict_vars = list(wt = 3))

  # Deliberate steps instead of a continuous feel
  slice_explore(model, sliders = hp, slider_breaks = c(66, 110, 245))

  # A numeric predictor stepped through its observed values
  model2 <- lm(mpg ~ disp + hp + cyl, data = mtcars)
  slice_explore(model2, sliders = c(hp, factor(cyl)))

  # Pin cyl per color group, slide the rest, with a confidence ribbon
  slice_explore(model2, mapping = aes(color = factor(cyl)),
                interval = "confidence")
}

}
\seealso{
\code{\link[=autoplot.lm]{autoplot.lm()}} — \code{autoplot(model, sliders = TRUE)} is a shortcut
for this function; \code{\link[=geom_slice]{geom_slice()}}, which computes every curve shown;
\code{\link[=scatter_3d]{scatter_3d()}} for the 3-D surface view of a two-predictor model.
}
