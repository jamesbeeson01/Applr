% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/autoplot.lm.R
\name{autoplot.lm}
\alias{autoplot.lm}
\title{Automatically plot a linear model}
\usage{
\method{autoplot}{lm}(
  object,
  ...,
  data = NULL,
  mapping = NULL,
  type = c("2d", "3d"),
  x_axis = NULL,
  xaxis = NULL,
  sliders = NULL,
  slider_breaks = NULL,
  slider_steps = NULL
)
}
\arguments{
\item{object}{A linear model fitted by \code{\link[=lm]{lm()}} \strong{with a \code{data} argument} —
the data is recovered from the model, so \code{lm(y ~ x, data = your_data)}
works but \code{lm(your_data$y ~ your_data$x)} does not.}

\item{...}{Passed on to \code{\link[=geom_slice]{geom_slice()}} — for example
\code{predict_vars = list(hp = 110)} to choose the slice,
\code{interval = "confidence"} for a ribbon, or fixed aesthetics such as
\code{color = "red"} — or to \code{\link[=scatter_3d]{scatter_3d()}} when \code{type = "3d"}.}

\item{data}{A data frame to draw the scatter from instead of the data
recovered from the model — for example the model's data with extra
columns added for \code{mapping} aesthetics. Passed to \code{\link[ggplot2:ggplot]{ggplot2::ggplot()}}.
The rows must still match the data the model was fitted to
(\code{\link[=geom_slice]{geom_slice()}} checks, and errors on a mismatch).}

\item{mapping}{Extra aesthetics created with \code{\link[ggplot2:aes]{ggplot2::aes()}}, passed to
\code{\link[ggplot2:ggplot]{ggplot2::ggplot()}} — for example \code{mapping = aes(color = factor(cyl))}.
Entries named \code{x} or \code{y} override the automatically chosen axes.}

\item{type}{\code{"2d"} (default) for a \code{\link[=geom_slice]{geom_slice()}} ggplot, or \code{"3d"} for an
interactive \code{\link[=scatter_3d]{scatter_3d()}} surface (needs exactly two numeric predictors).}

\item{x_axis}{The name of the predictor to place on the x-axis, such as
\code{x_axis = "disp"}. Defaults to the model's first numeric predictor.
Ignored when \code{type = "3d"}.}

\item{xaxis}{Deprecated; use \code{x_axis} instead.}

\item{sliders}{If given (and not \code{FALSE}), return an interactive
\code{\link[=slice_explore]{slice_explore()}} widget instead of a static ggplot. \code{TRUE} puts a slider
on every predictor the plot does not show; or name them — bare names
(\code{sliders = c(disp, cyl)}), strings, or \code{factor(cyl)} for steps at the
observed values. Cannot be combined with \code{type = "3d"}.}

\item{slider_breaks, slider_steps}{Slider grids for \code{sliders}, passed to
\code{\link[=slice_explore]{slice_explore()}}: \code{slider_breaks} gives exact (intentionally jumpy)
step values, \code{slider_steps} the number of steps in a continuous grid.}
}
\value{
A ggplot of the model's data with a slice of the model drawn
through it; a plotly surface from \code{\link[=scatter_3d]{scatter_3d()}} when \code{type = "3d"}; or
an interactive slider widget from \code{\link[=slice_explore]{slice_explore()}} when \code{sliders} is
given.
}
\description{
\code{autoplot()} turns a fitted \code{\link[=lm]{lm()}} into a complete ggplot in one call: the
data the model was fitted to as a scatter, with a \code{\link[=geom_slice]{geom_slice()}} prediction
line through it. The result is a regular ggplot, so it can be extended with
\code{+} as usual (labels, scales, more layers).
}
\details{
Pass \code{type = "3d"} for a model with exactly two numeric predictors to get an
interactive \code{\link[=scatter_3d]{scatter_3d()}} surface instead of the 2-D slice; \code{...} is then
forwarded to \code{\link[=scatter_3d]{scatter_3d()}} (e.g. \code{n}, \code{colors}).

Pass \code{sliders = TRUE} (or name predictors, e.g. \code{sliders = c(disp, cyl)})
for an interactive \code{\link[=slice_explore]{slice_explore()}} widget instead: the same plot, with
one slider for each predictor the plot does not show, moving the slice in
real time. \code{slider_breaks} and \code{slider_steps} control the slider grids;
see \code{\link[=slice_explore]{slice_explore()}} for details.

The model's \emph{first numeric} predictor goes on the x-axis (override with
\code{x_axis}) and the raw response variable goes on the y-axis. Everything else
is handled by \code{\link[=geom_slice]{geom_slice()}} and reported on the console: predictors not
visible on the plot are imputed (mean for numeric, most common value for
factor/character), and a transformed response such as \code{lm(log(y) ~ x)} is
automatically back-transformed to match the raw y-axis.
}
\examples{
library(ggplot2)

# A complete plot from a model in one call
autoplot(lm(mpg ~ disp, data = mtcars))

# Predictors not on the plot are imputed (a message says how)
autoplot(lm(mpg ~ disp + hp, data = mtcars))

# Choose which predictor goes on the x-axis
autoplot(lm(mpg ~ disp + hp, data = mtcars), x_axis = "hp")

# Transformed response, back-transformed onto the raw mpg axis
autoplot(lm(log(mpg) ~ disp, data = mtcars))

# Options pass through to geom_slice(); the result is a normal ggplot
autoplot(lm(mpg ~ disp + hp, data = mtcars),
         predict_vars = list(hp = 110), interval = "confidence") +
  labs(title = "Slice at hp = 110")

# Custom data and extra aesthetics for the scatter
autoplot(lm(mpg ~ disp, data = mtcars),
         data = mtcars, mapping = aes(color = factor(cyl)))

# An interactive 3-D surface for a two-predictor model
if (interactive()) {
  autoplot(lm(mpg ~ disp + hp, data = mtcars), type = "3d")
}

# Interactive sliders for the predictors the plot does not show
if (interactive()) {
  autoplot(lm(mpg ~ disp + hp + wt, data = mtcars), sliders = TRUE)
}

}
\seealso{
\code{\link[=geom_slice]{geom_slice()}}, which draws the line and handles everything
slice-shaped; \code{\link[=slice_explore]{slice_explore()}} for the slider view; \code{\link[=scatter_3d]{scatter_3d()}} for
the interactive surface; and \code{\link[=geom_slice_subtitle]{geom_slice_subtitle()}} /
\code{\link[=geom_slice_text]{geom_slice_text()}} to annotate the result.
}
