---
title: "Dev TODOs"
output: html_document
date: "2025-09-20"
---

# Priority


- [x] Add standards to `decisions.Rmd` (like error format)
- [ ] Simplify context in CLAUDE.md - move to either skill or testing/README.md
- [x] Make sure existing ground truth tests are accurate
- [ ] Check for comprehensiveness of testing suite and edge cases

- [ ] Refactor geom_slice for cleanness, consistency, and readability - some notes in `known_issues.Rmd` and `decisions.Rmd`
  - [ ] Follows ggplot conventions - note in `known_issues.Rmd`
  - [ ] Solid error handling
    - [ ] Error messages consistent with style guide in decisions.Rmd
    - [ ] Roll out the `slice_*()` condition helpers (`R/conditions.R`) across the package — replace direct `stop()`/`warning()`/`message()` calls so all user-facing conditions follow the format in `decisions.Rmd`. Start with the non-conforming `geom_slice.R` message ("Value for ... not specified").
  - [ ] Fix argument-name and other naming violations to match `decisions.Rmd` (e.g. axis args like `xaxis`, `color`/`colors`), adding deprecation aliases for any public arguments that change.
- [ ] Get geom_slice working under the most common conditions
  - [ ] Any y transformation
  - [x] An x axis matching its first variable
  - [ ] Grouping
  - [ ] Faceting
  - [ ] A third invisible variable (y ~ x + x2)

- [x] Separate multi-plot test files
- [ ] Test `scatter_3d`
- [ ] Fix remaining errors in `known_issues.Rmd`
- [ ] Update code examples in README (particularly geom_slice)

## Queue

- [ ] Test use case of ggplot(data, aes(x, log(y))) + geom_slice(lm)
- [ ] Also test ggplot(...) + ... + scale_y_log() or something like that
- [ ] Add error check to `geom_slice` and `slice_2d` checking if the data being plotted in the ggplot is the same as the data the lm was fit to
- [ ] Consider if the names `slice_2d` and `geom_slice` convey their function best
- [ ] Migrate fully to snake_case (e.g. `xaxis` → `x_axis`, `color`/`colors`), dropping the current public-argument exceptions in `decisions.Rmd`. Requires: (1) a deprecation plan for renamed public arguments (aliases + warning/`lifecycle`, removal timeline), and (2) updating the naming-conventions standard once settled.
- [ ] Remove %>% in favor of |>
- [ ] Tidy repo in general...

## New Features

- [ ] `autoplot(lm)` (takes minimal arguments, feasts/ggtime is a good example)
- [ ] Add confidence and prediction interval to `geom_slice` (similar to `geom_fit`)
- [ ] Add an interval to `slice_2d` (like `geom_fit`)
- [ ] Allow passing a range of values for a slice, plotting multiple lines in one function (ie x2=c(1,2,3),x3=c(1,4) plots 6 lines)
- [ ] Allow specifying impute method — min / max / median (numeric) / custom — instead of the default mean; add `gs_` test cases for each when implemented. (Current impute types — numeric -> mean, factor/character -> mode — are covered by `gs_17`/`gs_18`.)
<!-- Deferred - not well specified/explained
- [ ] Add a 'band' function (or add to geom_slice). A good example (if I say so myself) is in `~/testing/student_models/beesonjames_LATE_664773_146922498_HouseSellingPrices-1.html`
- [ ] New function for auto-labeling geom_slice lines (above is also one example)
-->